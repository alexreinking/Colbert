//In Colbert, there are no data types; They are converted between each other as the semantics request.

func y(var)
{
	var = var**2-var+1;
	return var;
}

func main() //As in many programming languages, the main function is called first.
{
	let num = 7;
	str = y(num);
}
